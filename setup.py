"""Setup script for AETHER offline multimodal evidence RAG system."""

from setuptools import setup, find_packages

setup(
    name="aether",
    version="1.0.0",
    description="Offline multimodal evidence RAG system",
    author="AETHER Team",
    packages=find_packages(),
    python_requires=">=3.11",
)
